# Test configuration
# You can add test executables here
# For example:
rdma_loopback = executable('rdma_loopback', 'rdma_loopback.cpp',
    include_directories: inc,
    dependencies: app_dep,
)
test('rdma_loopback', rdma_loopback) 
